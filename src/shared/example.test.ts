// npx jest --watch
describe('Test', () => {
  test('should return true when ', () => {
    expect(true).toBe(true);
  });
  test('should return 0 when 10%2', () => {
    expect(10%2).toBe(0);
  })
})
